<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">JanSeva App</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Home</a>
        </li>
        <% if logged_in? %>
         <li class="nav-item" aria-current="page">
         <%= link_to 'Logout',logout_path,method: :delete,class: "nav-link active" %>
         </li>
         <% if current_user.usid? || current_user.admin? %>
          <li class="nav-item" aria-current="page">
         <%= link_to 'Openings',openings_path,class: "nav-link active" %>
           </li>
           <%end%>
           <% if !current_user.admin? %>
           <% if !current_user.usid? %>
           <li class="nav-item" aria-current="page">
         <%= link_to 'Add More Details',new_userdetail_path,class: "nav-link active" %>
           </li>
           <% else %>
             <li class="nav-item" aria-current="page">
         <%= link_to 'Edit Details',edit_userdetail_path(current_user.usid),class: "nav-link active" %>
           </li>
             <li class="nav-item" aria-current="page">
         <%= link_to 'View Profile',user_path(current_user),class: "nav-link active" %>
         </li>
           <% end %>
           <% end %>
      
        <%else%>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/signup">SignUp</a>
        </li>
         <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/login">Login</a>
        </li>
        <%end%>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/about">About</a>
        </li>
      </ul>
    </div>
  </div>
</nav>